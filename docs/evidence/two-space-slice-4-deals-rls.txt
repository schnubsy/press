SLICE 4 — press_deals gating proof (ADDITIVE phase; secret redacted throughout)
Date: 2026-09-19T02:14:47Z
sync_id (redacted): 1722…8e13  (128-bit, DB-generated, never committed; full value in operator scratchpad + handed to Mark)
anon publishable key: sb_publishable__iv3bI7xDAs…FS4uX

APPLIED (migration history): 20260918_press_deals_gate_additive
  - added column sync_id text, DEFAULT = the DB-generated value, backfilled all 15 rows (0 nulls, 1 distinct)
  - added header-gated policies press_deals_sel / _ins / _upd (x-plan-id = sync_id), mirroring press_fsa
  - the open policies press_deals_anon_all / _auth_all are still present ON PURPOSE (see CLEANUP)

POSITIVE — anon GET WITH the x-plan-id header returns the card-scout rows (gate admits the owner):
  rows via header: 15
  sample (item_id, brand): [{"item_id":"d-ff9f78e8","brand":"Uber"}, 
 {"item_id":"d-972435fc-57baf7","brand":"DoorDash"}, 
 {"item_id":"d-51237707","brand":"DoorDash"}]

Amendment B (agent auto-stamp) — a service-role insert with NO sync_id supplied was auto-stamped
  by the column DEFAULT (stamped_by_default = true). This IS the deployed agent's write path
  (service_role, no explicit sync_id), so the next live cron tick lands a correctly-stamped row.
  => safe to SET NOT NULL at cleanup.

PRE-CLEANUP state (deliberate): anon GET with NO header still returns rows (15) because the
  open policy press_deals_anon_all is still active. This keeps the LIVE production card-scout page
  (which does not yet send x-plan-id) rendering until the arc close republishes it.

CLEANUP (deferred to arc close — Amendment A step 6, sequenced AFTER card-scout.html republish):
  migration 20260918_press_deals_gate_cleanup:
    drop policy press_deals_anon_all;  drop policy press_deals_auth_all;
    revoke delete on public.press_deals from anon, authenticated;
    alter table public.press_deals alter column sync_id set not null;
  THEN the negative probes are captured here (append):
    - no header  => 0 rows
    - wrong header (0000000000000000000000000000dead) => 0 rows
    - correct header => own rows only
    - anon DELETE => rejected
  and get_advisors(security) is re-checked clean.

================================================================================
CLEANUP APPLIED — 2026-09-19  (migration 20260918_press_deals_gate_cleanup,
                               Supabase version 20260919043403, project eepjhpyziczrxvirczio)
Precondition MET: private-wing passkey enrolment succeeded and Card Scout renders
deals through the vault (Mark confirmed). Ran AFTER card-scout.html republished with x-plan-id.

Statements applied (DDL):
  drop policy if exists press_deals_anon_all on public.press_deals;
  drop policy if exists press_deals_auth_all on public.press_deals;
  revoke delete on table public.press_deals from anon, authenticated;
  alter table public.press_deals alter column sync_id set not null;   -- DEFAULT kept

Post-state (service-role introspection):
  policies now: press_deals_ins, press_deals_sel, press_deals_upd  (open _anon_all/_auth_all GONE)
  sync_id: is_nullable=NO, DEFAULT still present
  anon/authenticated DELETE grant: revoked ([])
  rows: 15 total, 0 null sync_id, 1 distinct sync_id, 0 stray probe rows

NEGATIVE PROBES (live anon publishable key sb_publishable__iv3bI7…2yyFS4uX; plan id masked 1722…8e13):
  1. no x-plan-id header        => HTTP 200, body [], Content-Range */0            (ZERO rows)     PASS
  2. mismatched header (0000…dead)
       GET                      => HTTP 200, body []                               (ZERO rows)     PASS
       INSERT (no body sync_id) => HTTP 401, 42501 "new row violates row-level
                                   security policy for table press_deals"          (REJECTED)      PASS
  3. matching header 1722…8e13  => HTTP 200, 15 rows, Content-Range 0-14/15                         PASS
       (own rows only: table holds exactly 1 distinct sync_id over 15 rows, so the
        15 returned are all-and-only this capability's rows)
  4. anon DELETE (header 1722…8e13, filter item_id=eq.__no_such_item__)
                                => HTTP 401, 42501 "permission denied for table
                                   press_deals" (DELETE grant revoked)             (REJECTED)      PASS
  5. enumerate: select=* no header => HTTP 200, body [], Content-Range */0         (NOT LISTABLE)  PASS

get_advisors(security) re-run: NO new lints from this cleanup. press_deals is clean (no finding).
Remaining lints are pre-existing and unrelated to this arc:
  - rls_enabled_no_policy (INFO): public.press_capture_seen, public.press_capture_state (agent-health capture tables)
  - security_definer_view (ERROR): press_agent_runs_daily_public, press_agent_health_public,
    press_agent_runs_daily_all_public, press_agent_runs_public (public read-only agent-health dashboard views)

Live card-scout.html re-verified serving after cleanup (see history.md). The 🔴 open-policy finding is CLOSED.
================================================================================
